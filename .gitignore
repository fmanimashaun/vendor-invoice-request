node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
*.local
test-output.pdf

# Letterhead artwork and fonts are not in git — they are generated by
# scripts/extract-assets.py and downloaded from Google Fonts. See README.
assets/*.png
assets/*.ttf
assets/fonts/
assets/*/

# Per-deployment seed data. Copy scripts/seed.example.sql to this and edit it;
# nothing organisation-specific belongs in the repository.
scripts/seed.local.sql

# Deployment configuration: resource ids, Access team domain, AUD tag.
# Copy wrangler.example.toml to wrangler.toml and fill in your own.
wrangler.toml

# One-off production repair scripts. Deployment-specific, never committed.
*.sql.local
